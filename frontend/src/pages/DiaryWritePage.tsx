// 지금은 "이 파일이 존재한다"는 사실 자체가 중요한 단계임.
// 실제 폼(제목/내용/감정/위치 입력창)은 다음 단계에서 채운다.
export default function DiaryWritePage() {
  return (
    <div className="min-h-screen bg-[#f8f9fa] p-6">
      <h1 className="text-xl font-bold text-ink">일기 작성 (준비 중)</h1>  
    </div>
  );  
}
